enyo.ready(function () {
	enyo.kind({
		name: "Bootplate.HomeController"
		, kind: "Bootplate.ParentController"
		, data: {}
    , handlers: {
      onSomething: 'something'
    }
    // signup
    , something: function () {
    }
	});
});
